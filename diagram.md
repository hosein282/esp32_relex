+-------------------+
|    User Devices   |
| (Web, MQTT, SMS)  |
+--------+----------+
         |
         v
+-------------------+         +-------------------+
|    WiFi Network   |<------->|    MQTT Broker    |
+--------+----------+         +-------------------+
         |                               ^
         v                               |
+-------------------+                   |
|      ESP32        |-------------------+
|-------------------|                   |
|  - WiFi           |                   |
|  - SIM800 (GSM)   |<------------------+
|  - LittleFS       |                   |
|  - AsyncWebServer |                   |
|  - TinyGSM        |                   |
|  - PubSubClient   |                   |
|  - Relays/Inputs  |                   |
+--------+----------+                   |
         |                              |
         v                              |
+-------------------+                   |
|   Remote Storage  |                   |
|   (LittleFS)      |                   |
+--------+----------+                   |
         |                              |
         v                              |
+-------------------+                   |
|   Physical Relays |                   |
+-------------------+                   |
         |
         v
+-------------------+
|   Sensors/Inputs  |
+-------------------+
         |
         v
+-------------------+
|   SMS/Call Logic  |
+-------------------+
         |
         v
+-------------------+
|   Alert/Feedback  |
+-------------------+
Legend/Explanation:

User Devices: Interact via web (HTTP), MQTT, or SMS.
WiFi Network: Main network for ESP32 to connect to MQTT broker and serve web requests.
MQTT Broker: Central message hub for MQTT communication.
ESP32: Core controller, handles WiFi, GSM (SIM800), file storage (LittleFS), web server, MQTT, and relay/input logic.
Remote Storage (LittleFS): Stores remote codes, scenarios, and configuration.
Physical Relays: Controlled by ESP32 outputs.
Sensors/Inputs: Physical inputs monitored by ESP32.
SMS/Call Logic: Handles SMS sending/receiving and call alerts via SIM800.
Alert/Feedback: Sends notifications or feedback to users (via MQTT, SMS, or web).
Data Flows:

User Devices <-> ESP32: via HTTP, MQTT, or SMS
ESP32 <-> MQTT Broker: via WiFi or GSM (TinyGSM)
ESP32 <-> Relays/Inputs: direct GPIO control
ESP32 <-> Remote Storage: via LittleFS
ESP32 <-> SMS/Call Logic: via SIM800
Alerts/Feedback: sent to users as needed


راهنمای کاربری و راه‌اندازی کنترلر هوشمند ESP32
معرفی کلی
این دستگاه یک کنترلر هوشمند چندمنظوره است که با استفاده از ماژول ESP32، قابلیت اتصال به اینترنت از طریق وای‌فای و سیم‌کارت (GSM)، کنترل رله‌ها، دریافت و ارسال پیامک، ارتباط MQTT، سنسور دما و ورودی/خروجی‌های متنوع را فراهم می‌کند. همچنین امکان مدیریت از طریق وب، پیامک و اپلیکیشن‌های MQTT را دارد.

امکانات اصلی
اتصال به اینترنت از طریق وای‌فای یا سیم‌کارت (GSM)
کنترل و مانیتورینگ رله‌ها و PWM
دریافت و ارسال پیامک (SMS)
ارتباط با سرور MQTT برای اتوماسیون و مانیتورینگ
مدیریت سناریوها و زمان‌بندی‌ها
ذخیره‌سازی اطلاعات روی حافظه داخلی (LittleFS)
نمایشگر OLED برای نمایش وضعیت
دریافت فرمان از ریموت 433MHz
مدیریت از طریق وب‌سرور داخلی
راه‌اندازی اولیه
تغذیه و اتصالات سخت‌افزاری

تغذیه ۵ ولت به برد ESP32 و ماژول SIM800 متصل کنید.
سنسور دما DS18B20 را به پین ۲۳ متصل کنید.
رله‌ها و ورودی‌ها را طبق نقشه به پین‌های تعریف‌شده وصل کنید.
سیم‌کارت را در ماژول SIM800 قرار دهید.
اتصال به وای‌فای

پس از روشن شدن دستگاه، اگر اطلاعات وای‌فای ذخیره نشده باشد، دستگاه به حالت Access Point می‌رود.
با موبایل یا لپ‌تاپ به شبکه وای‌فای با نام مشابه RELEX-XX متصل شوید.
در مرورگر آدرس 192.168.4.1 را وارد کنید.
اطلاعات وای‌فای (SSID و رمز عبور) و سرور MQTT را وارد و ذخیره کنید.
دستگاه به صورت خودکار ریستارت و به وای‌فای متصل می‌شود.
اتصال به سرور MQTT

پس از اتصال به اینترنت، دستگاه به سرور MQTT (آدرس پیش‌فرض: 5.238.178.210 یا broker.emqx.io) متصل می‌شود.
موضوعات (topic) پیش‌فرض:
ارسال: report
دریافت: action
می‌توانید با اپلیکیشن‌هایی مانند MQTT Explorer یا Node-RED به این سرور متصل شوید.
مدیریت از طریق وب

پس از اتصال به شبکه، آدرس IP دستگاه را در سریال مانیتور مشاهده کنید.
با وارد کردن این IP در مرورگر، به صفحه مدیریت دستگاه دسترسی خواهید داشت.
کنترل رله‌ها و ورودی‌ها
از طریق وب‌سرور:
با مراجعه به صفحه وب دستگاه، می‌توانید رله‌ها را روشن/خاموش کنید یا نام آن‌ها را تغییر دهید.
از طریق MQTT:
با ارسال پیام به تاپیک action با فرمت مناسب، می‌توانید رله‌ها را کنترل کنید.
از طریق پیامک:
با ارسال پیامک به شماره سیم‌کارت دستگاه، می‌توانید رله‌ها را روشن/خاموش کنید یا سناریوها را اجرا کنید.
از طریق ریموت 433MHz:
با ثبت ریموت جدید، می‌توانید رله‌ها را با ریموت کنترل کنید.
سناریوها و زمان‌بندی
افزودن سناریو:
از طریق وب یا پیامک می‌توانید سناریوهای مختلف (مثلاً روشن شدن رله در دمای خاص یا زمان خاص) تعریف کنید.
زمان‌بندی رله‌ها:
می‌توانید برای هر رله تایمر یا برنامه زمان‌بندی تعریف کنید تا در ساعات خاصی روشن یا خاموش شود.
پیامک‌ها و مدیریت شماره‌ها
ثبت شماره مدیر:
با ارسال پیامک p1=0912xxxxxxx به دستگاه، شماره شما به عنوان مدیر ثبت می‌شود.
دریافت وضعیت:
با ارسال پیامک stat یا p، وضعیت رله‌ها و سنسورها را دریافت می‌کنید.
حذف شماره:
با ارسال پیامک del=1 شماره اول حذف می‌شود.
حذف همه شماره‌ها:
با ارسال پیامک del=all همه شماره‌ها حذف می‌شوند.
بروزرسانی (OTA)
در صورت ارسال دستور بروزرسانی از طریق MQTT یا وب، دستگاه به صورت خودکار فایل جدید را دانلود و نصب می‌کند.
نمایشگر OLED
نمایش وضعیت شبکه (وای‌فای یا GPRS)
نمایش دما، سیگنال، وضعیت رله‌ها و ورودی‌ها
نکات مهم
در صورت فراموشی رمز وای‌فای:
دکمه فیزیکی را بیش از ۱۲ ثانیه نگه دارید تا تنظیمات وای‌فای ریست شود.
در صورت قطع اینترنت وای‌فای:
دستگاه به صورت خودکار از طریق سیم‌کارت به اینترنت متصل می‌شود.
در صورت قطع برق:
تنظیمات و سناریوها در حافظه داخلی ذخیره می‌شوند و پس از روشن شدن مجدد بازیابی می‌شوند.
خطاها و رفع اشکال
عدم اتصال به وای‌فای:
از صحیح بودن SSID و رمز عبور مطمئن شوید و دستگاه را ریست کنید.
عدم اتصال به سرور MQTT:
آدرس سرور را بررسی کنید و از فعال بودن اینترنت اطمینان حاصل کنید.
عدم دریافت پیامک:
سیم‌کارت را بررسی و از فعال بودن سرویس پیامک مطمئن شوید.
دستورات پیامکی پرکاربرد
دستور پیامک	عملکرد
p1=0912xxxxxxx	ثبت شماره مدیر
stat	دریافت وضعیت رله‌ها
r1on	روشن کردن رله ۱
r1off	خاموش کردن رله ۱
del=1	حذف شماره اول
del=all	حذف همه شماره‌ها
sch	دریافت لیست زمان‌بندی‌ها
temp	دریافت دمای فعلی
wipe	ریست کامل دستگاه
نکات امنیتی
رمز وای‌فای و شماره مدیر را در اختیار افراد غیرمجاز قرار ندهید.
در صورت گم شدن یا سرقت دستگاه، با ارسال پیامک wipe اطلاعات را پاک کنید.
پشتیبانی
در صورت نیاز به راهنمایی بیشتر یا بروز هرگونه مشکل، با پشتیبانی فنی تماس بگیرید.

موفق باشید!
این راهنما را می‌توانید به عنوان فایل PDF یا چاپی برای کاربران نهایی ارائه دهید.
در صورت نیاز به راهنمای تخصصی‌تر یا توضیح بخش خاصی، لطفاً اعلام کنید.